<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:camel="http://camel.apache.org/schema/spring"
	xsi:schemaLocation="
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://camel.apache.org/schema/spring http://camel.apache.org/schema/spring/camel-spring.xsd">

	<!-- Define a traditional camel context here -->
	<camelContext id="camel" xmlns="http://camel.apache.org/schema/spring">
		<route>
			<from
				uri="twitter://search?keywords=OpenShiftRoadmap&amp;type=polling&amp;delay=10000"></from>
			<to uri="bean:myTransformer?method=transform" />
			<log message=">>> ${body}" />
		</route>
		<route>
			<from uri="file:/Users/rettori/openshift/web-console/apps/twitter/twitter/queue" />
			<setHeader headerName="queueName">
				<simple>${body}</simple>
			</setHeader>
			<log message="${headers.queueName}"/>
		</route>
	</camelContext>

</beans>
